<div class="row">
  <div class="col-md-8">
    <h2>Detail Contact</h2>
    <% if(!contact) { %>
    <div class="alert alert-danger fw-bold" role="alert">
      Data Contact tidak ada.<br />
      <a href="/contact" class="alert-link fw-normal">&laquo; Kembali ke daftar Contact</a>.
    </div>
    <% }else { %>
    <div class="card mt-3" style="width: 18rem">
      <div class="card-body">
        <h5 class="card-title"><%= contact.name; %></h5>
        <h6 class="card-subtitle mb-2 text-muted"><%= contact.email; %></h6>
        <p class="card-text"><%= contact.nohp; %></p>
        <a href="/contact/edit/<%= contact.name %>" class="btn btn-info btn-sm">Ubah</a>
        <a href="/contact/delete/<%= contact.name %>" onclick="return confirm('yakin ingin menghapusnya?')" class="btn btn-danger btn-sm">Hapus</a><br />
        <a href="/contact" class="card-link">&laquo; Kembali ke daftar contact</a>
      </div>
    </div>
    <% } %>
  </div>
</div>
